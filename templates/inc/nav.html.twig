<nav class="navbar navbar-expand-lg navbar-light col-sm-12 w-100">
    <a href="{{ path('main_home') }}"><h1 id="logo" class="nav-h1">Sortir.com</h1></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">

        {% if app.user %}
            <li class="nav-item active"><a class="nav-link" href="{{ path('main_home') }}" title="Home">Accueil</a></li>
            <li class="nav-item active"><a class="nav-link" href="{{ path('profil_MonProfil') }}" title="Profil">Mon Profil</a></li>
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item active"><a class="nav-link" href="{{ path('admin_ville') }}" title="City">Villes</a></li>
                <li class="nav-item active"><a class="nav-link" href="{{ path('admin_campus') }}" title="Campus">Campus</a></li>
                <li class="nav-item active"><a class="nav-link" href="{{ path('admin_dashboard') }}" title="Dashboard">Dashboard</a></li>
            {% endif %}
            <li class="nav-item active"><a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Se déconnecter</a></li>
        {% endif %}
    </ul>
    </div>
</nav>

